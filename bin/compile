#!/usr/bin/env bash
# bin/compile BUILD_DIR CACHE_DIR ENV_DIR

BUILD_DIR=$1

echo "Deleting node_modules..."
rm -r $BUILD_DIR/node_modules

echo "Deleting target/node_modules"
rm -r $BUILD_DIR/target/node_modules

echo "Cleanup done..."
